<div class="index_courses">
  <p id="notice"><%= notice %></p>

  <h1>Courses</h1>
  <hr/>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Resolution Number</th>
        <th>Modality</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @courses.each do |course| %>
        <tr>
          <td><%= course.name %></td>
          <td><%= course.no_resolution %></td>
          <td><%= course.modality.name %></td>
          <td><%= link_to t(:show), course, class: "btn_show" %></td>
          <% if current_user.role.name == 'Admin' %>
            <td><%= link_to t(:edit), edit_course_path(course), class: "btn_edit" %></td>
          <% end -%>
          <% if current_user.role.name == 'Teacher' %>
            <td><%= link_to t(:add_modules), new_course_course_module_path(course) %></td>
            <td><%= link_to t(:list_modules), course_course_modules_path(course) %></td>
          <% end -%>
               

          <!-- <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </tr>
      <% end %>
    </tbody>
  </table>
  <br>

  <tr>
    <% if current_user.role.name == 'Admin' %>
      <td><%= link_to t(:new_course), new_course_path, class: "new_course" %></td>
    <% end -%>
    <td><%= link_to t(:back), root_path, class: "back" %></td>
  </tr>
</div>
