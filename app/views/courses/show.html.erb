<p id="notice"><%= notice %></p>
<h1>Courses</h1>
<p>
  <strong>Name:</strong>
  <%= @course.name %>
  <br>
  <strong>Number resolution:</strong>
  <%= @course.no_resolution %>
</p>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
