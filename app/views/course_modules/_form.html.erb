<%= form_for([@course, course_module]) do |f| %>
  <% if course_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_module.errors.count, "error") %> prohibited this course_module from being saved:</h2>

      <ul>
      <% course_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :youtube_link %>
    <%= f.text_field :youtube_link %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
